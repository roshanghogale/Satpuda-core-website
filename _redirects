# Do not map /page -> /page.html here.
# Cloudflare Pages already serves page.html at /page and redirects .html -> clean URLs.
# Those mappings create a redirect loop with CF's HTML extension handling.

/downloads /download 301
/home / 301
